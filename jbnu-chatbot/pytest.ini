[pytest]
testpaths = tests
# 실사이트 스모크는 기본 실행에서 뺀다. 네트워크가 필요하고 느리며,
# 원천이 바뀌면 우리 코드와 무관하게 빨간불이 된다.
#   전체:   python -m pytest tests -m "not smoke"   (기본)
#   스모크: python -m pytest tests -m smoke
addopts = -m "not smoke"
markers =
    smoke: 실사이트에 실제로 붙는 테스트. 별도 주기로 돌린다.
