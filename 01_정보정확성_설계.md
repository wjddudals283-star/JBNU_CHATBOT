# 전북대 제58대 전심 총학생회 카카오톡 챗봇 — 정보 정확성 설계 (v2)

작성일: 2026-08-10 · v2 개정
개정 사유: (1) 챗봇의 정체성을 **학생 생활 전반 종합 안내**로 재정의. 식단은 12개 도메인 중 하나. (2) 카카오 챗봇 관리자센터 공식 규격을 확인해 아키텍처를 단순화.

---

## 0. 이 챗봇은 무엇인가

**"총학생회에 물어볼 만한 모든 것"에 답하는 창구다.**

학생이 총학에 묻는 건 식단이 아니라 이런 것들이다. 등록금 분납 언제까지야, 휴학하려면 뭐 내야 해, 열람실 몇 시까지 열어, 셔틀 막차 언제야, 공약 그거 어떻게 됐어, 동아리방 대관 어떻게 해, 화장실 고장났는데 어디에 말해.

이 질문들의 공통점은 **답이 어딘가에 분명히 있는데 학생이 그걸 찾기 귀찮거나 어디 있는지 모른다**는 것이다. 챗봇의 가치는 새 정보를 만드는 게 아니라 흩어진 정보를 정확하게 꺼내오는 데 있다.

그래서 설계의 중심 질문은 "무엇을 답할까"가 아니라 **"어디까지 답해도 되는가"**다.

---

## 1. 도메인 지도

| # | 도메인 | 대표 질문 | 정보 공급 티어(§3) |
|---|---|---|---|
| D1 | 학사 | 수강신청 언제야 / 휴학 어떻게 해 / 졸업요건 | T1·T3 |
| D2 | 등록·재정 | 등록금 분납 / 장학금 신청 / 국가장학금 | T1·T3 |
| D3 | **총학생회** | 공약 어떻게 됐어 / 예산 어디 썼어 / 집행부 연락처 | **T4** |
| D4 | 학생활동 | 동아리 등록 / 총투표 / 대동제 일정 | T1·T4 |
| D5 | 식음 | 후생관 오늘 메뉴 / 카페 운영시간 | T1·T2 |
| D6 | 시설·공간 | 열람실 몇 시까지 / 강의실 대관 / 체육관 | T2·T4 |
| D7 | 교통 | 통학버스 시간표 / 셔틀 / 주차 | T1·T2 |
| D8 | 주거 | 기숙사 입사신청 / 기숙사 식단 / 외박신청 | T1·T3 |
| D9 | 복지·지원 | 학생증 재발급 / 상담센터 / 우산 대여 / 제휴 할인 | T3·T4 |
| D10 | 취업·진로 | 취업 프로그램 / 공모전 | T1 |
| D11 | **민원·제보** | 시설 고장 / 인권·성폭력 / 건의 | **특별취급(§7)** |
| D12 | 공지·행사 | 교내공지 / 총학공지 / 행사 일정 | T1 |

식단(D5)은 12분의 1이다. 데모로 먼저 만드는 이유는 중요해서가 아니라 **파이프라인 전체를 가장 짧게 한 바퀴 돌려볼 수 있는 도메인이라서**다. 갱신 주기가 빠르고, 원천이 명확하고, 틀리면 바로 티가 난다. 정확성 아키텍처를 시험하기에 좋은 시험대다.

---

## 2. 정확성 아키텍처 — 3층 분리

```
[1] Source Layer   원문 스냅샷 그대로 보관 (HTML/PDF/이미지 + fetched_at + content_hash)
        ↓  parse / 입력
[2] Fact Layer     스키마 정형 데이터. 모든 레코드가 출처·관측시각·유효기간·신뢰도를 갖는다
        ↓  query (검증됨 AND 신선함인 것만)
[3] Answer Layer   템플릿에 값만 꽂는다. 생성 모델은 여기 못 들어온다
```

**층을 넘는 규칙**

- 1→2: 검증 게이트(§5)를 통과해야 넘어간다. 실패하면 `quarantine`으로 격리되어 답변에서 영구 제외.
- 2→3: 신선도 게이트(§6)를 통과해야 넘어간다. 실패하면 값 대신 "확인 실패 + 원문 링크".

두 게이트가 "정확한 정보만"의 실질적 보증이다. 프롬프트로 부탁하는 게 아니라 경로를 막는다.

---

## 3. 정보 공급 4티어 — 이 프로젝트의 실제 난이도

챗봇 데이터를 "크롤하면 된다"고 생각하면 절반만 만들 수 있다. 정보마다 성격이 다르고, 성격마다 공급 방식이 다르다.

| 티어 | 성격 | 예시 | 공급 방식 | TTL |
|---|---|---|---|---|
| **T1** | 자주 바뀌는 공개 데이터 | 식단, 공지, 학사일정, 셔틀시간표 | 자동 크롤 | 6~24h |
| **T2** | 가끔 바뀌는 공개 데이터 | 운영시간, 시설·연락처, 위치 | 자동 크롤 + 개정 감시 | 30~180d |
| **T3** | 문서 안에 갇힌 데이터 | 학칙·회칙 PDF, 장학 요강, 신청 절차 | 1회 구조화 + 사람 검수 + 원문 해시 감시 | 개정 시 |
| **T4** | **총학만 아는 데이터** | 공약 이행률, 사업 진행, 제휴업체, 대여물품, 행사, FAQ | **집행부 어드민 직접 입력** | 항목별 지정 |

### T4가 이 챗봇의 절반이다

D3(총학생회) 도메인 전체와 D9(복지) 상당수는 **웹 어디에도 없다.** 크롤할 대상이 존재하지 않는다.
"공약 3번 어떻게 됐어?"에 답하려면 총학 집행부가 진행 상황을 입력해야 한다.

그래서 **집행부용 어드민이 크롤러만큼 중요한 산출물**이다. 그리고 T4도 같은 게이트를 거친다.

```
T4 레코드 필수 필드
  author        입력한 사람
  approved_by   승인한 사람 (국장급 이상)
  valid_to      NOT NULL ★ 만료일 없는 T4 레코드는 저장 불가
  review_due    재검토 기한
```

`valid_to`를 강제하는 이유. 사람이 입력한 정보는 **사람이 잊어버린다.** 학기 초에 넣은 "제휴 카페 10% 할인"이 학기 말에도 그대로 답변에 나가면, 그게 챗봇이 거짓말하는 가장 흔한 경로다. 만료일이 지나면 자동으로 답변에서 빠지고 담당자에게 알림이 간다.

### T3의 함정

학칙 PDF를 한 번 파싱해서 넣어두면 편하다. 문제는 **원문이 개정됐을 때 아무도 모른다는 것**이다.
그래서 T3는 구조화된 데이터와 별개로 **원문 URL의 content_hash를 주기적으로 감시**한다. 해시가 바뀌면 해당 도메인의 모든 T3 레코드를 `needs_review`로 전환하고, 검수 전까지 답변은 "규정이 최근 개정됐어요. 원문을 확인해 주세요"로 대체한다.

---

## 4. 온톨로지 스키마

### 4.1 식별자 규칙

결정론적 IRI. 같은 것을 두 번 수집해도 같은 ID가 나와야 upsert가 되고 중복이 없다.

```
jbnu:org/총학생회                     jbnu:place/후생관
jbnu:outlet/후생관-푸드코트            jbnu:facility/중앙도서관-제1열람실
jbnu:procedure/휴학신청                jbnu:policy/총학회칙#제12조
jbnu:pledge/58/03                     jbnu:service/우산대여
jbnu:notice/총학생회/2026-08-03/등록금심의위원회
jbnu:meal/2026-08-10/후생관-푸드코트/lunch
```

### 이력을 쌓는 테이블은 식별자에 시점이 있어야 한다 ★

원래 규칙은 "같은 것은 같은 ID"였다. 그런데 **같은 대상의 다른 시점 관측은 다른 레코드**다. 시점이 ID에 없으면 이튿날 크롤에서 PK 충돌로 크롤 자체가 죽는다.

실제로 `operating_hours`가 그랬다. `UNIQUE`에는 `valid_from`이 있었지만 ID 생성 규칙에는 없었다. 같은 날 재크롤은 자연키 `ON CONFLICT`가 받아내서 **하루 동안은 멀쩡했다.**

```
jbnu:hours/후생관-푸드코트#학기중                    ✗ 시점 없음
jbnu:hours/후생관-푸드코트#unspecified@2026-08-10    ✓
```

**같은 점검이 필요한 테이블**: `menu_price`(단가 개정), `pledge_progress`(진행상황 갱신), `procedure`(요강 개정), `operating_hours`.
불변인 것(`notice`, `meal_service` — 이미 날짜가 ID에 있음)은 해당 없다.

> 이 유형은 **하루 뒤에 터진다.** 당일 테스트로는 절대 안 잡힌다. 시계열 테이블을 만들 때는 반드시 **다른 날짜로 두 번 이상 넣어보는 테스트**를 둔다.

### 4.2 코어 엔티티

**Organization** — 정보의 책임 주체
```
id, name, aliases[], type(총학생회|단과대학생회|생협|생활관|본부부서|동아리연합회),
parent_org_id, official_url, contact_id
```

**Place / Facility** — 장소와 그 안의 이용 단위
```
Place:    id, name, aliases[], campus, building, floor, lat, lng, entrance_note
Facility: id, name, aliases[], place_id, facility_type(열람실|강의실|체육시설|식당|카페|사무실),
          capacity, reservation_required, reservation_url
```
Outlet(식당·카페)은 Facility의 하위 유형이다. 식단 전용 스키마를 따로 두지 않는다.

**OperatingHours** — 시설·시설군 공통
```
id, target_id(Facility|Outlet), term(학기중|방학|시험기간|공휴일),
weekday, open_time, close_time, break_start, break_end, note
```

**Procedure** — 신청 절차 ★D1·D2·D8·D9의 뼈대
```
id, title, aliases[], owner_org_id, domain,
steps[]        {order, what, where, url}
required_docs[]
period_start, period_end          -- 신청 기간
eligibility, fee, contact_id, source_url
```
학생 질문의 상당수가 "○○ 어떻게 해?"다. 이건 사실 조회가 아니라 **절차 조회**다. 절차를 1급 엔티티로 두지 않으면 이 질문군 전체를 못 받는다.

**Policy** — 규정·회칙·학칙
```
id, issuer_org_id, doc_title, article, rule_text,
effective_from, effective_to, supersedes, source_url, source_page
```

**Pledge / PledgeProgress** — 총학 공약 ★T4, 이 챗봇의 차별점
```
Pledge:         id, term(58), number, category, title, summary, target_date
PledgeProgress: id, pledge_id, status(계획|진행중|완료|보류|철회),
                progress_note, evidence_url, updated_at, author, approved_by
```
공약 이행 현황을 챗봇으로 상시 공개하는 총학은 드물다. **투명성이 곧 기능이 된다.**

**Notice / Event**
```
id, issuer_org_id, title, body, published_at, period_start, period_end,
target_audience[], apply_url, supersedes, source_url
```
`supersedes`가 없으면 챗봇이 철 지난 공지를 자신 있게 읽어준다. 개정·정정 공지는 이전 것을 반드시 무효화한다.

**Service** — 복지·대여·상담 (T4)
```
id, name, aliases[], provider_org_id, service_type(대여|상담|할인|지원금),
how_to_use, location_place_id, hours_id, stock_status, partner_benefit, valid_to
```

**MealService / MenuItem** — 식단 (D5 전용)
```
MealService: id, outlet_id, date, meal_type(breakfast|lunch|dinner),
             service_status(operating|closed_temporary|unknown),
             corner, price_default
MenuItem:    id, meal_service_id, name, price, category, allergens[], display_order
```
> `service_status`가 식단 스키마의 핵심이다. "메뉴가 없음"에는 **원래 그 끼니를 안 함 / 오늘 쉼 / 아직 안 올라옴 / 크롤 실패** 네 가지가 있고 답변이 각각 달라야 한다.
> 다만 이 넷을 **파싱 시점에 구분할 수는 없다.** 빈 칸은 그냥 `unknown`으로 두고, 구분은 답변 시점에 운영시간과 조인해서 한다(§5 따름 원칙). `unknown`은 절대 "없다"고 답하지 않는다.

**Contact / AcademicCalendarEntry / Alias**
```
Contact:  id, org_id, label, phone, email, office_place_id, hours_note
Calendar: id, term, title, start_date, end_date, source_url
Alias:    surface_form, canonical_id, entity_type, weight
```

### 4.3 관계

```
Facility  --located_in-->   Place
Outlet    --operated_by-->  Organization
Procedure --owned_by-->     Organization
Pledge    --has_progress--> PledgeProgress
Policy    --supersedes-->   Policy
Notice    --supersedes-->   Notice
Service   --provided_by-->  Organization
*         --sourced_from--> SourceSnapshot
Alias     --refers_to-->    *
```

### 4.4 모든 Fact의 공통 출처 메타 (NOT NULL)

```
source_id / source_url / observed_at / valid_from / valid_to / confidence
extraction_method   html_selector | json_api | pdf_parse | vlm_ocr | manual_admin
status              verified | quarantine | needs_review | superseded | expired
tier                T1 | T2 | T3 | T4
```

DB 제약으로 `source_id NOT NULL`을 건다. **출처 없는 사실은 물리적으로 저장될 수 없다.**

---

## 5. 검증 게이트 (Fact Layer 진입 전)

1. **스키마 검증** — 필수 필드, 타입, enum
2. **도메인 규칙** — 가격 500~20,000 / 날짜 범위 / 문자열 길이 / 기간의 시작≤종료 / 전화번호 형식
3. **이상 탐지** — 파싱 0건인데 이전엔 정상(셀렉터 깨짐 의심) / 전 주기 대비 ±70% 변동 / 중복 과다
4. **정렬 앵커 검증** ★ — 파싱된 날짜·요일 헤더가 요청 범위와 **순서까지** 일치하는지
5. **교차 검증** — 원천 2개 이상이면 불일치 시 우선순위 채택 + `conflict` 플래그 (**식단은 1단계부터 활성**)
6. **T3/T4 전용** — 원문 해시 변경 감지 → `needs_review` / `valid_to` 누락 → 저장 거부

### 4번을 따로 둔 이유 — 게이트가 못 잡는 오류

실제로 겪은 사례. `selectolax`의 `row.css("td,th")`가 문서 순서를 지키지 않아 행 머리글이 끝으로 밀렸다.
**예외도 안 나고, 스키마도 맞고, 값도 정상 범위이고, 개수도 맞는데, 메뉴가 하루씩 밀렸다.** 1~3번 게이트를 전부 통과한다.

이런 오류는 **외부에서 검증 가능한 앵커**로만 잡힌다. 공휴일·선거일처럼 날짜가 이미 알려진 값을 픽스처에 넣고 정렬을 확인한다. (위 사례는 `[지방선거]`가 화요일에 걸린 걸로 발견했고, 바로잡으니 수요일 6/3 — 실제 지방선거일과 맞았다.)

일반 규칙: **"파싱 성공 + 값 정상 + 정렬 틀림"은 조용히 통과한다.** 값 검증과 별개로 구조 검증이 필요하다.

실사이트에 붙이자마자 이 게이트가 두 번째 건을 잡았다. 주 시작일을 문서의 첫 `date=` 링크에서 읽었는데 그게 **이전주 네비게이션 버튼**이라 표 전체가 7일 밀렸다. 개수 21건도 맞고 값도 정상이라 1~3번은 전부 통과했다.
→ 주 시작일은 독립 신호 셋(표시 기간 / 이전주+7 / 다음주−7)을 대조하고 하나라도 어긋나면 중단한다.

### 픽스처로는 못 잡는 부류가 있다

같은 페이지를 두 번 받았는데 해시가 달랐다. CSS 링크에 `?ver=<유닉스타임>` 캐시버스터가 초 단위로 박혀 있어서다.
바이트 해시로 비교하면 `unchanged`가 **영원히 성립하지 않아** 파서가 매번 돌고 스냅샷이 무한히 쌓인다.
그런데 픽스처는 바이트가 동일하므로 관련 테스트는 계속 초록불이다.

→ **시간·세션·실시간에 의존하는 결함은 픽스처가 원리적으로 못 잡는다.** 실사이트 대상 스모크 테스트를 별도 주기로 돌린다.
→ 비교는 변동분을 제거한 `stable_hash`로, **저장은 원문 그대로.** 정규화본을 저장하면 나중에 파서를 고쳐 재파싱할 때 원본이 없다.

### 원문 스냅샷은 시크릿을 품는다 ★

"저장은 원문 그대로"에는 대가가 있다. **원문에 세션 토큰이 섞여 들어온다.**

실제로 조사 산출물 `docs/probe/jbnu_cafeteria__browser_ua.html`에 CSRF 토큰과 `JSESSIONID`가 그대로 들어 있었다. 공개 저장소에 올렸으면 그대로 노출됐다.

**마스킹으로 해결하면 안 된다.** 지우는 순간 "원문 그대로"가 깨지고 재파싱 근거가 사라진다. 그래서 이렇게 나눈다.

| | 원문 유지 | 저장소 커밋 |
|---|---|---|
| 원문 스냅샷 `data/snapshots/` | ✅ | **❌ 절대** |
| 조사 산출물 `docs/probe/` | ✅ | **❌ 절대** |
| 테스트 픽스처 | 시크릿 제거본 | ✅ (개별 스캔 필수) |

원문은 **서버 디스크에만** 둔다. 저장소·백업·공유 어디에도 올리지 않는다.
커밋될 픽스처는 스냅샷에서 따로 만들고, 만들 때 시크릿이 없는지 파일 단위로 확인한다.

> 일반화: **"원본을 그대로 보존한다"와 "저장소에 올린다"는 다른 결정이다.** 둘을 묶으면 사고가 난다.

### 셀렉터가 깨졌을 때
기존 데이터를 지우지 않는다. 새 데이터를 넣지도 않는다. **stale 표시만 하고 둔다.**
챗봇은 신선도 게이트에 걸려 "확인 실패"를 답한다. 조용히 틀린 답을 하는 것보다 낫다.

### 추론으로 채운 값은 사실이 아니다 ★

게이트만큼 중요한 원칙이다. 세 가지 적용례.

| 유혹 | 왜 안 되는가 | 규칙 |
|---|---|---|
| 단가표 이름을 유사도로 식단표에 매칭 | `계란라면`과 `신라면`은 다른 상품이다. 붙이는 순간 없는 가격을 만든다 | **정확 일치만.** 미매칭은 `NULL` |
| 명시된 미운영을 행 없이 표현 | 부재로 적으면 "안 함"과 "안 긁음"이 같은 모양이 된다 | **부정도 관측이다. 행으로 남긴다**(`is_closed=1`) |
| 방학이니까 식당도 쉰다고 판단 | 후생관은 방학에도 점심을 돈다(실측). 시설·끼니마다 다르다 | 학사일정으로 유추 금지 |
| 빈 칸을 `closed_vacation`으로 저장 | 빈 칸은 관측값이 아니라 **관측의 부재**다. 같은 빈 칸이 원천마다 뜻이 다르다 | `unknown`으로 저장 |
| 가격 범위를 하한 단일값으로 표기 | 6,000~6,500원을 "6,000원"이라 하면 학생이 모자란 돈을 들고 간다 | 원문 표기 그대로 |

조인·유추·보간은 전부 추론이다. 결정론적으로 확실할 때만 하고, 아니면 빈칸으로 둔다.

### 따름 원칙 — 추론은 저장하지 않는다 ★

위 표의 2·3행은 같은 실수의 두 얼굴이다. 둘 다 **파싱 시점에 다른 원천을 끌어와 결론을 내려는 것**이다.

문제는 그렇게 만든 결론이 `status='verified'` 레코드로 굳어지면, 그 뒤로는 **관측된 사실과 구분이 안 된다**는 점이다. "이 `closed_vacation`은 눈으로 본 건가 추측한 건가"를 아무도 답할 수 없게 된다.

> **저장하는 건 관측. 결합은 질의 계층에서 매번 다시 한다.**

이러면 두 가지가 좋아진다. 원천별 사실이 오염되지 않고, 나중에 운영시간 데이터가 좋아지면 **재크롤 없이 답변이 개선된다.**
`menu_price`를 `menu_item`에서 분리한 것과 정확히 같은 이유다. 조인은 질의 시점의 일이다.

적용 예 — 식단 빈 칸의 B분기 판정

| `meal_service` | `serves_meal()` | 답변 |
|---|---|---|
| `operating` | — | A 메뉴 |
| `closed_temporary` (원천 명시) | — | B "오늘은 쉬어요" |
| `unknown` | `False` | B "그 끼니는 운영하지 않아요" |
| `unknown` | `True` | C-1 "아직 올라오지 않았어요" |
| `unknown` | `None` | C-2 "확인하지 못했어요" |

`serves_meal()`이 데이터 없을 때 `False`가 아니라 `None`을 돌려주는 게 핵심이다. **모를 때 "안 해요"라고 답하는 것도 추론이다.**

### 불확실할 때 어느 쪽으로 틀릴 것인가 ★

추론을 완전히 없앨 수는 없다. 근거가 부분적일 때 어느 방향으로 기울지를 정해둬야 한다.

**기준은 오답의 비용이다. 헛걸음을 만드는 쪽이 훨씬 나쁘다.**

| 잘못된 답 | 결과 |
|---|---|
| 안 하는데 "한다"고 함 | 학생이 갔는데 문이 닫혀 있다. **우리가 보낸 것이다** |
| 하는데 "모르겠다"고 함 | 학생이 원문 링크로 직접 확인한다 |

그래서 **긍정 단정에는 높은 근거를, 부정·유보에는 낮은 근거를 요구한다.** 대칭이 아니다.

적용례 — 운영시간표에 학기 정보가 없을 때(실제 사례). 방학에 운영이 줄지 늘지는 않으므로,
그 시간표에서 나온 **"안 한다"는 방학에도 성립**하지만 **"한다"는 성립하지 않을 수 있다.**
따라서 부정 결론은 채택하고 긍정 결론은 "모름"으로 강등한다.

> 이 논거에 남은 구멍 하나. 단조성은 **그 시간표가 학기중 것일 때만** 성립한다. 만약 크롤한 게 방학 시간표인데 지금이 학기중이면 부정 결론도 틀릴 수 있다.
> 사이트가 term 없이 하나만 게시하면 통상 학기중(최대) 시간표라 위험은 작지만, 가정인 건 맞다. 그래서 아래 §의 "근거를 보여준다"가 이 구멍을 메운다 — 학생이 시간표를 직접 보면 축소된 것인지 알아챌 수 있다.
> 학기 경계에서 같은 시설의 시간표가 바뀌는 걸 관측하면, 그때 term 의존성이 **관측으로** 확인된다. 학사일정 없이도 시간이 지나면 해소된다.

### 불확실성을 고백하지 말고, 근거를 보여준다 ★

답변에 "○○ 기준이라 정확하지 않을 수 있어요" 같은 문구를 붙이고 싶어질 때가 있다. 대부분 붙이면 안 된다.

**caveat는 사용자의 행동을 바꿀 때만 쓴다.** 행동이 안 바뀌는 단서는 소음이고, 소음이 쌓이면 진짜 경고까지 무시된다. 더 나쁘게는, 우리가 옳다고 판단한 결론에 스스로 의심을 심어 학생을 헛걸음시킨다.

대신 **판단의 근거가 된 관측을 그대로 보여준다.**

```
✗  "조식은 운영하지 않아요. 학기 구분 없는 시간표 기준이에요."
✓  "조식은 운영하지 않아요.
    운영시간은 평일 점심 11:30–14:00이에요. (8/10 확인)"
```

단서는 검증할 수 없지만 관측은 읽는 사람이 검증할 수 있다. 시간표가 이상하면 학생이 먼저 알아챈다.

**그리고 두 번째 이유가 있다. 근거를 나란히 놓으면 결론과의 모순이 눈에 보인다.**

실제로 이 규칙을 적용하자마자 이런 답변이 나왔다.
```
후생관은 점심은 운영하지 않아요.
운영시간은 평일 점심 11:30–14:00이에요.    ← 점심을 한다고 말하고 있다
```
판정은 맞았다(그 날이 일요일). 틀린 건 문장이었다 — "그 날 안 함"과 "그 끼니를 아예 안 함"을 구분하지 못했다.
근거를 안 보여줬으면 이 모순은 드러나지 않았다. **근거 표시는 사용자를 위한 배려이자 우리 자신을 위한 자기검사다.**

### "없다"는 항상 최소 두 가지 뜻을 가진다 ★

이 프로젝트에서 잡은 오류가 계속 같은 모양이었다.

| 마주친 "없음" | 갈라야 했던 뜻 |
|---|---|
| 식단표 빈 칸 | 미운영 / 미게시 / 크롤 실패 |
| 운영시간표에 행 없음 | 그 끼니 안 함 / 아직 안 긁음 |
| `not_offered` | 그 날 쉼 / 그 끼니를 아예 안 함 |
| 가격 미매칭 | 무료 / 단가표에 이름이 다름 |

**"없다"를 만나면 반드시 최소 두 갈래로 나눌 수 있는지 먼저 물어라.** 나누지 않고 하나로 처리하면, 그 코드는 조용히 틀린 답을 내는 지점이 된다.

### 테스트는 규격을 보고 사람은 문장을 본다

위 모순은 **테스트가 잡지 못했다.** 응답 JSON 규격 검증(T8)은 통과했다. 스키마도 맞고 필드도 다 있었다.
문장의 의미가 틀린 건 렌더 결과를 **읽어야** 보인다.

→ **새 분기를 만들면 실데이터로 한 번 렌더해서 눈으로 보기 전까지 완료가 아니다.** 픽스처 통과는 완료 조건이 아니다.

---

## 6. 신선도 게이트 (답변 직전)

| 타입 | max_staleness | 초과 시 |
|---|---|---|
| MealService | 24h | 확인 실패 + 원문 링크 |
| Notice | 6h | 최신 공지 목록 링크로 대체 |
| PledgeProgress | 30d | 값 + "○월 기준" 명시 |
| OperatingHours | 30d | 값 + 기준일 명시 |
| Procedure / Policy | 개정 감시 | `needs_review`면 원문 확인 안내 |
| Contact / Place | 180d | 값 + 확인 안내 |
| Service (T4) | `valid_to` | 만료 시 자동 제외 + 담당자 알림 |

---

## 7. D11 민원·제보 — 특별취급

이 도메인만 규칙이 다르다. **챗봇이 상담하려 들면 안 된다.**

| 유형 | 처리 |
|---|---|
| 시설 고장·불편 | 접수 폼 링크 + 담당 부서 연락처 |
| 일반 건의 | 총학 건의 채널 연결 |
| **인권·성폭력·괴롭힘** | **즉시 사람 연결.** 전문 상담 창구 연락처만 안내하고 대화를 이어가지 않는다 |
| 신변 위협·긴급 | 긴급 연락처 즉시 노출 |

### 연락처 검증 — 배포 차단 조건 ★

**안전 분기에서 틀린 번호를 안내하는 건 답을 안 하는 것보다 나쁘다.**
이 도메인의 모든 번호는 **총학이 각 기관에 직접 전화해서 확인한 것만** 배포한다. 웹 검색 결과도 근거로 부족하다.

확인된 것(2026-08 웹 확인, **총학 재확인 필요**)
```
전북대 인권센터          063-270-3025   진수당 154호
전북대 행복드림센터      063-219-5301~3 진수당 1층  ← 심리상담. '학생상담센터'가 아님
여성긴급전화             1366           24시간
자살예방상담전화         109            24시간 (2024년 1393에서 통합)
긴급                     112 / 119
```

> 이 항목들은 `config/safety_contacts.yaml`에 두고 코드에 박지 않는다. 번호가 바뀌면 배포 없이 고칠 수 있어야 한다.
> 미확인 번호가 하나라도 남아 있으면 **안전 분기를 배포하지 않는다.** 그 상태면 "총학에 직접 문의" 한 줄만 내보내는 게 맞다.

### 검증 이력의 날조가 값의 날조보다 나쁘다 ★

`verified: true` 하나로는 부족하다. **셋이 다 있어야 확인으로 친다.**
```
verified: true
verified_at:      확인한 날짜
verified_by:      확인한 사람 (직책)
verified_method:  phone | official_site
```
절반만 채워져 있으면 조용히 미확인으로 강등하지 않고 **예외를 던진다.** 절반만 채운 항목은 누군가 확인하다 멈춘 흔적이고, 그 상태로 배포되는 게 가장 위험하다.

**왜 값보다 이력이 더 위험한가.**
틀린 번호는 언젠가 드러난다. 누가 전화를 걸어보고 "이 번호 안 되는데요" 하면 된다.
그런데 **확인 이력을 지어내면 그 항목은 "이미 확인된 것"으로 분류되어 다시는 점검 대상에 오르지 않는다.** 오류가 자기 자신을 숨긴다.

그래서 확인자가 없으면 이름을 채우지 않는다. 날짜도 마찬가지다. **모르면 비워둔다.** 이건 §5 "추론으로 채운 값은 사실이 아니다"의 메타 버전이다 — 사실뿐 아니라 **사실의 출처도** 지어내면 안 된다.

### 확인 수준을 등급으로 기록한다

이분법으로 두면 "웹으로 봤는데 전화라고 적기"의 유혹이 생긴다. 있는 그대로 적을 수 있게 등급을 둔다.

| `verified_method` | 신뢰 | 적용 |
|---|---|---|
| `phone` | 최상 | 부서 개편 가능성이 있는 **교내 기관**은 이것으로 |
| `official_site` | 충분 | 109·1366·1331 같은 **전국 대표번호**는 이것으로 족하다 |
| (웹 검색 스니펫) | **불가** | 근거로 치지 않는다 |

인권·성폭력 관련 발화는 **인텐트 분류 이전 단계에서 가로챈다.** 정확성 문제가 아니라 안전 문제이고, 잘못된 안내의 대가가 다른 도메인과 비교가 안 된다. 이 분기는 절대 자동화 대상이 아니다.

---

## 8. 답변 계약과 역할 분담 ★v2 핵심 변경

관리자센터 공식 문서를 확인한 결과, **오픈빌더가 이미 NLU를 한다.** 블록(=인텐트) 매칭, 엔티티 추출, ML+ 유의어 학습이 플랫폼에 내장돼 있고, 추출된 값이 `action.params` / `detailParams`로 스킬서버에 전달된다. `sys.date` 같은 시스템 엔티티는 "금요일"을 실제 날짜까지 계산해서 준다.

그래서 별도 LLM 라우터를 만드는 건 중복이다. 역할을 이렇게 나눈다.

```
오픈빌더  : 발화 → 블록(인텐트) 매칭 + 엔티티 추출        [플랫폼이 담당]
스킬서버  : params → 온톨로지 조회 → 게이트 → 템플릿 렌더  [순수 결정론]
LLM      : 폴백 로그 분석, 별칭 후보 제안, 발화 예시 생성  [오프라인 개선용]
```

**결과적으로 실시간 답변 경로에 생성 모델이 아예 없다.** "결정론 우선"이 설계 방침이 아니라 구조적 사실이 된다.

```
답변 = 템플릿[block_id] ⊗ facts

facts = query(ontology, action.params)
        WHERE status = 'verified'
          AND now() - observed_at <= max_staleness[type]
          AND (valid_to IS NULL OR valid_to >= today)
```

### 답변 3분기

| 상황 | 응답 |
|---|---|
| 사실 있음 + 신선함 | 값 + 출처 링크 + 기준 시각 |
| 사실 없음(명확한 미운영·미해당) | "운영하지 않아요" + 대안 안내 |
| 사실 불명(unknown·stale·quarantine·needs_review) | "확인하지 못했어요" + 원문 링크 + 문의처 |

세 번째가 있어야 챗봇이 거짓말을 안 한다. **모른다고 말할 수 있는 챗봇이 정확한 챗봇이다.**

### 카카오 UI 제약 (공식 규격, 반드시 준수)

| 항목 | 제한 |
|---|---|
| `outputs` | 1~3개 |
| `quickReplies` | 최대 10개 |
| `simpleText` | 1,000자 (500자 초과 시 "전체 보기" 버튼) |
| **`listCard.items`** | **최대 5개** (케로셀 4개) ★ 초과분은 잘림 |
| `textCard` | title 50자 / description 400자 |
| `basicCard` | title 50자 / description 230자 / thumbnail 필수 |
| 버튼 | 가로 최대 2개(라벨 8자) / 세로 최대 3개(라벨 14자) |
| 케로셀 | 최대 10개 (listCard는 5개) |
| 스킬 응답 | `version: "2.0"` 필수 |

listCard 5개 제한이 실무에서 자주 걸린다. 메뉴가 7개면 5개 + "전체 보기" 버튼으로 끊어야 한다.

---

## 9. 인텐트 체계

### 네이밍
```
<도메인>.<행위>.<대상>
```

### 인텐트 우선순위 ★수요조사 반영

**1순위 — 수요 상위. 리스트 메뉴·웰컴 블록에 노출**
```
deadline.upcoming      "지금 마감 임박한 거 뭐 있어?"  ★불편 1위 대응
calendar.date          "수강신청 언제야?" "개강 언제야?"   (수요 1·3위)
finance.deadline       "장학금 신청 언제까지야?"          (수요 2위)
notice.latest          "총학 공지 뭐 있어?"
```

**2순위 — 이미 만들어진 것 / 부수 기능**
```
food.menu.today   food.hours.outlet   food.price
facility.hours    facility.location
council.pledge.status   council.contact
```

**3순위 — 원천 확보 후**
```
notice.search.topic   procedure.howto   transport.schedule   event.upcoming
```

**항상**
```
report.facility        report.safety  ★§7 특별취급
fallback.unknown
```

`deadline.upcoming`이 이 챗봇의 대표 인텐트다. 불편 1위가 "중요한 공지를 놓친다"(67.6%)인데 알림을 보내지 않기로 했으므로, **채널에 들어온 사람이 반드시 보게 되는 자리**에 이걸 둔다.

### 슬롯
```
outlet, facility, place, date, meal_type, topic,
organization, procedure_name, pledge_number, route, deadline_window
```

### 원천 없는 인텐트는 만들지 않는다
답할 데이터가 없는데 인텐트만 만들면 그게 환각의 출발점이다.
예상질문 리스트를 받으면 각 질문에 `intent / slots / 필요 엔티티 / 티어 / 원천 확보 여부`를 태깅하고, **원천 미확보 질문은 인텐트가 아니라 백로그로 보낸다.**

### 추천질문(quickReplies) 3층 배치
1. **웰컴 블록** — 도메인 대표 5개. 챗봇의 범위를 첫인상으로 알린다
2. **답변 직후** — 같은 도메인의 이웃 질문 3개
3. **폴백 직후** — 지원 범위를 알려주는 대표 4개. 이탈 방지의 핵심 지점

---

## 10. 자동 업데이트 파이프라인

```
스케줄러 → fetch → content_hash 비교
                    ├ 동일 → observed_at만 갱신 (파싱 스킵)
                    └ 변경 → parse → 검증 게이트 → upsert → 신선도 갱신
                                        └ 실패 → quarantine + 알림

T3 감시  → 원문 hash 변경 → 해당 도메인 needs_review + 검수 알림
T4 만료  → valid_to 경과 → 자동 제외 + 담당자 알림
```

| 대상 | 주기 |
|---|---|
| 식단 | 매일 06:00 / 10:30 / 16:00 |
| 공지 (교내·학생·총학) | 1시간 |
| 학사일정·셔틀 | 주 1회 |
| 운영시간·시설 | 주 1회 |
| T3 원문 해시 감시 | 일 1회 |
| T4 만료 점검 | 일 1회 |

**무인 운영 3원칙**
1. **하트비트** — 24시간 성공 크롤이 없으면 관리자 알림. 침묵이 가장 위험하다
2. **셀렉터 외부화** — `selectors.yaml`. 사이트 개편 시 YAML 한 줄로 복구
3. **폴백** — 1차 실패 시 2차 원천, 둘 다 실패면 stale 유지 + 거절 응답

---

## 11. 데이터 원천 검증 — 완료 (2026-08-10)

**식단 이미지는 0건. 세 원천 모두 구조화돼 있다.** VLM 파싱 분기는 폐기.

| 원천 | 방식 | 특징 | 역할 |
|---|---|---|---|
| 생협 | `POST /function/get_cafeteria_menu.php` (JSON) | 기본 UA로 200. 쿠키·Referer 불필요. 날짜 지정 가능 | **1차** |
| 학교 | `POST .../cafeteria/dataAjax.do` `type=day` (HTML 조각) | X-CSRF-Token 필수. **date 파라미터 없음 → 이번 주 고정** | 2차 + 가격·운영시간 |
| 생활관 | `inner.php?sMenu=B7100&date=` | 서버 렌더 테이블 | 기숙사 식당 |
| ~~대학생협 데이터센터~~ | 표가 비어 있음 | — | 폐기 |

`extraction_method`는 `json_api` / `html_selector`, `confidence = 0.95`.

### 여기서 나온 세 가지 설계 변경

1. **원천이 둘이다** → 같은 주 식단을 독립 제공하므로 §5-5 교차검증을 **1단계부터 켠다**. 1차 생협, 불일치 시 1차 채택 + `conflict` 플래그.
2. **가격은 T1이 아니라 T2다** → 가격이 날짜가 아니라 **품목명**에 붙어 있고, 그 이름이 식단표와 다르다(`등심돈까스`↔`통등심돈까스`). `menu_price` 테이블로 분리하고 정확 일치만 조인한다. 답변에서도 메뉴 카드와 가격 카드를 분리한다.
3. **코너가 2단이다** → 후생관은 하루 13조합. `zone`(한식/찌개*돌솥/일품) + `corner`(백반/찌개/돌솥) 2단으로 두고 UNIQUE에 포함한다. 둘 다 `NOT NULL DEFAULT ''` — NULL이면 UNIQUE가 무력화된다.

### 원천이 없어서 만들지 않는 것

- **"다음 주 메뉴"** — 45일을 훑었으나 이번 주 월~금 이후는 전부 빈 배열. §9 원칙에 따라 백로그.
  단, 폴백으로 보내지 말고 **"아직 안 올라왔어요"**로 답한다. 게시 시점을 관측하면 "보통 ○요일에 올라와요"까지 붙일 수 있다.

> 웹에 떠도는 후생관 가격(스페셜오므라이스 3,800원 등)은 다이닝코드·테이블링 같은 **비공식 3자 출처**다. `source_type='thirdparty'`로 답변에서 자동 제외한다.

---

## 11-2. 수요조사 결과와 그 함의 (N=37, 2026-06~08)

### 숫자

| 가장 필요한 기능 (3개 선택) | |
|---|---|
| 수강신청 일정 안내 | 62.2% |
| 장학금·등록금 안내 | 59.5% |
| 학사일정 알림 | 56.8% |
| 교내 행사·축제 | 43.2% |
| 취업·비교과 | 35.1% |
| 총학생회 공지 | 32.4% |
| FAQ 자동응답 | 5.4% |
| 캠퍼스 생활 정보 | 5.4% |

| 가장 불편한 점 (복수) | |
|---|---|
| 중요한 공지를 놓친다 | 67.6% |
| 공지가 너무 많아 찾기 어렵다 | 62.2% |
| 여러 플랫폼을 확인해야 한다 | 56.8% |
| 모바일에서 불편 | 37.8% |

이용 의향 '매우 있음' 83.8%, 개설 필요 '매우 필요' 67.6%. 정보 획득 경로는 인스타그램 73%, 학과 단톡방 59.5%, 학교 홈페이지 48.6%.

### 이 데이터의 용도 — 주제 우선순위

**전달 방식이 아니라 주제 순위를 정하는 데 쓴다.** 알림(push)은 하지 않는다. 챗봇은 전부 pull이다.

수요 순위를 도메인에 매핑하면 이렇다.

| 순위 | 응답 | 도메인 | 티어 |
|---|---|---|---|
| 1 | 수강신청 일정 62.2% | D1 학사 | T1 |
| 2 | 장학금·등록금 59.5% | D2 재정 | T1 + T3 |
| 3 | 학사일정 56.8% | D1 학사 | T1 |
| 4 | 교내 행사·축제 43.2% | D4 / D12 | T1 + T4 |
| 5 | 취업·비교과 35.1% | D10 | T1 |
| 6 | 총학생회 공지 32.4% | D3 / D12 | T1 + T4 |

**D1과 D2가 압도적 1순위다.** 식단(D5)이 아니다.

### "언제"와 "어떻게"를 나눠라 ★

D1·D2는 T3(문서에 갇힌 데이터)가 많아 보여서 비싸 보인다. 그런데 질문을 쪼개면 다르다.

| 질문 | 필요한 것 | 티어 | 난이도 |
|---|---|---|---|
| "수강신청 **언제**야?" | 학사일정 날짜 | T1 | 크롤로 해결. 싸다 |
| "수강신청 **어떻게** 해?" | 절차·요건 | T3 | 문서 파싱 + 검수. 비싸다 |
| "장학금 마감 **언제**야?" | 공고 기간 | T1 | 싸다 |
| "나 장학금 **받을 수 있어**?" | 자격 판정 | — | **하지 않는다**(§개인 판정 금지) |

설문 문항이 "수강신청 **일정** 안내", "장학금 및 등록금 **안내**"다. **수요는 '언제' 쪽에 있다.**
그래서 1순위 수요의 대부분이 T1 크롤로 커버된다. 비싸지 않다.

### push 없이 "놓침"을 줄이는 방법

불편 1위가 "중요한 공지를 놓친다"(67.6%)인데 알림을 안 보내기로 했다. 그럼 어떻게 하나.

**채널을 여는 순간 마감을 보여준다.** 웰컴 블록과 리스트 메뉴가 그 자리다. 둘 다 무료다.

```
리스트 메뉴(채팅방 하단 상시 노출)
  [마감 임박]  [학사일정]  [장학·등록금]  [총학 공지]
```

`deadline.upcoming` 인텐트를 1순위로 만든다. "지금 마감 임박한 게 뭐야?"에 신청 기간이 남은 항목을 마감 가까운 순으로 보여준다.
학생이 물어봐야 한다는 한계는 있지만, **채널에 들어온 사람은 반드시 보게 되는 자리**에 두면 상당 부분 메워진다.

불편 2·3위("너무 많아 찾기 어렵다" 62.2%, "여러 플랫폼" 56.8%)는 애초에 챗봇이 잘하는 일이다. 검색과 통합 조회가 pull의 본령이다.

### 이 데이터로 알 수 없는 것 ★

우리 원칙을 우리 데이터에 적용한다.

- **선택지에 "식단"이 없었다.** 8개 보기 중 어디에도 없다. 그러니 "캠퍼스 생활 정보 5.4%"를 **식단 수요 없음으로 읽으면 안 된다.** 물어보지 않은 것은 관측되지 않은 것이다.
- **설문이 push를 전제로 물었다.** "카카오톡으로 받아볼 수 있다면 이용 의향" — 챗봇(pull) 수요는 측정되지 않았다. 유일한 pull 항목인 FAQ 자동응답이 최하위인 것도, 3개만 고르는 구조에서 마감 있는 항목에 밀린 결과일 수 있다.
- **N=37, 그리고 응답자 편향.** 인스타그램이 1위(73%)인 걸 보면 총학 인스타로 배포됐을 가능성이 크다. 이미 총학 채널에 관심 있는 층이다.

→ 이 조사는 **push 채널 수요를 확인했고, 챗봇 수요는 측정하지 않았다.** 둘은 다른 질문이다.
→ 챗봇 인텐트 우선순위는 이 데이터가 아니라 **폴백 로그**로 정한다(2단계 이후).

## 12. 단계 계획

**운영 기간이 확정됐다. 2026-11-30 (제58대 임기 종료)까지. 다음 집행부에 인계하지 않는다.**
오늘(8/10) 기준 **16주**. 알림(push)은 하지 않는다 — 전부 pull이다.

### 학사 달력이 로드맵을 결정한다

| 날짜 | 사건 | 챗봇 관점 |
|---|---|---|
| **8/18~21** | 2학기 수강신청 | 수요 1위 이벤트. **이번 임기에 남은 유일한 수강신청** |
| **9/1** | 개강 | 학생이 돌아온다. **오픈 목표일** |
| 9월 | 수강정정 · 등록금 분납 | |
| 10월 | 중간고사 · 교내 행사 | |
| 11월 | 장학금 · **차기 총학 선거** | 선거 안내는 T4 가치가 크다 |
| **11/30** | 임기 종료 | 서비스 종료 |

**8/18 수강신청은 포기한다.** 8일 뒤인데 배포도 학사일정 크롤러도 없다. 급하게 내서 틀리면 "정확한 정보만"이라는 정체성이 첫 노출에서 깨진다. 게다가 채널 친구 수가 아직 적어 열어도 아무도 모른다.

### 일정

| 기간 | 범위 |
|---|---|
| **8/10~8/24 (2주)** | Render 배포 · D1 학사일정 크롤러 · `deadline.upcoming` · 카카오 시나리오/블록 구성 |
| **8/25~8/31 (1주)** | 봇 테스트 · 문안 검수 · **안전 번호 5건 확인** · 채널 연결 |
| **9/1** | **개강 오픈 + 홍보** |
| 9월~10월 | D2 장학·등록금 · D12 공지 확장. **폴백 로그 보며 인텐트 증설** |
| 11월 초 | **기능 동결(freeze).** 새 기능 중단, 안정화만 |
| 11/20경 | 종료 안내 블록 노출 |
| **11/30** | 챗봇-채널 연결 해제 · **Render 결제 해지** |

**11월 초 동결이 중요하다.** 임기 끝 직전에 기능을 넣으면 검증할 시간이 없고, 문제가 생겨도 고칠 사람이 곧 사라진다.

**결제 해지를 잊는 것이 가장 흔한 실수다.** 자동 갱신이므로 11/30을 캘린더에 걸어둔다. 총 결제는 4개월 ≈ 3~4만원.

### 포기하는 것

기간 안에 못 한다. 하지 않는다고 명시한다.
```
T3 절차·규정 구조화 (휴학·복학 "어떻게")
D6 시설 · D7 교통 · D8 주거
개인화 (내 학과 공지만 등)
```

### 종료가 안전한 이유

크롤이 멈춰도 **신선도 게이트 때문에 자동으로 "확인하지 못했어요 + 원문 링크"로 떨어진다.**
마지막 데이터를 최신인 척 계속 뱉는 일이 구조적으로 불가능하다. 정확성을 위해 만든 장치가 **종료 안전장치 역할까지 한다.**

### 인계하지 않지만 남기기는 한다

코드와 데이터를 GitHub에 남겨두는 데는 비용이 들지 않는다. 인계할 의무를 만드는 게 아니라, **차기 집행부가 원할 때 선택할 수 있게** 두는 것뿐이다. 저장소를 지우지만 않으면 된다.

식단(D5)은 0단계에서 **파이프라인 검증용**으로 만들었고 그 역할은 끝났다. 갱신이 빠르고 틀리면 바로 티가 나서 게이트를 시험하기 좋은 도메인이었다. 실제 배포 우선순위는 수요조사가 가리키는 쪽이다.

4단계가 이 챗봇이 다른 대학 챗봇과 갈라지는 지점이다. 크롤만 하면 학교 정보 안내 봇이고, T4까지 하면 **총학생회 챗봇**이 된다.

---

## 참고 출처

- [카카오 챗봇 관리자센터 튜토리얼](https://kakaobusiness.gitbook.io/main/tool/chatbot/tutorial/make_chatbot/tutorial_1)
- [스킬 응답 타입별 JSON 포맷](https://kakaobusiness.gitbook.io/main/tool/chatbot/skill_guide/answer_json_format)
- [카카오 챗봇 이용요금 무료 전환](https://www.kakaocorp.com/page/detail/9756)
- [전북대 식단안내](https://www.jbnu.ac.kr/web/unvrslife/campuslife/cafeteria.do) · [생협 주간식단](https://coopjbnu.kr/menu/week_menu.php) · [생활관 주간식단](https://likehome.jbnu.ac.kr/home/main/inner.php?sMenu=B7100)
