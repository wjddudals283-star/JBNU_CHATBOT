# 클로드코드 작업지시서 — 전북대 총학 챗봇 1단계 (v2)

v2 개정: (1) 챗봇 범위를 **종합 안내**로 재정의 — 식단은 12개 도메인 중 1개. 스키마를 전 도메인 확장 가능하게. (2) 오픈빌더가 NLU를 담당하는 것을 확인 → **NLU 라우터 자체 구현 폐기**. (3) 카카오 공식 UI 제약 반영.

설계 근거는 `01_정보정확성_설계.md`, 카카오 개설 절차는 `03_카카오_개설_실행가이드.md`.
이 문서는 **구현 계약**이다. 필드명·enum·응답 형태는 여기 적힌 것을 그대로 쓴다. 바꿔야 하면 먼저 알린다.

---

## 0. 원천 조사 — 완료 (2026-08-10)

조사 결과 확정. **VLM 파싱 분기는 전부 폐기.** 세 원천 모두 구조화돼 있고 식단 이미지는 0건이다.

| 원천 | 방식 | 특징 | 역할 |
|---|---|---|---|
| **생협** | `POST /function/get_cafeteria_menu.php` (JSON) | 기본 UA로 200. 쿠키·Referer 불필요. **날짜 지정 가능** | **1차 원천** |
| **학교** | `POST /web/unvrslife/campuslife/cafeteria/dataAjax.do` `type=day` | HTML 조각. **X-CSRF-Token 필수**(없으면 403). **date 파라미터 없음 → 이번 주 고정** | 2차 + 가격·운영시간 |
| 생활관 | `inner.php?sMenu=B7100&date=` | 서버 렌더 테이블 | 기숙사 식당 |

```
extraction_method = json_api (생협) / html_selector (학교·생활관)
confidence        = 0.95
```

**CSRF 토큰 획득** — 학교 XHR은 먼저 `cafeteria.do`를 GET해서 `<meta name="_csrf">` 값을 읽고, 같은 세션으로 `X-CSRF-Token` 헤더에 실어 POST한다. 토큰은 세션당 발급되므로 `httpx.Client`를 재사용할 것.

**대조군 실측** — 8/10 생활관은 표 구조 정상, 칸만 비어 있음. T2 케이스가 실측으로 재현됐다.

### 어휘 매핑 (확정)

| 원천 | 표기 | → |
|---|---|---|
| 생협·학교 | 조식 / 점심 / 석식 | `breakfast` / `lunch` / `dinner` |
| 생활관 | 아침 / 점심 / 저녁 | 〃 |

`중식`은 원천에 없다. 사용자 발화용 별칭으로만 둔다.

`source_type` enum에 **`dorm` 추가**. 생활관은 공식 원천이므로 답변 허용 — 제외 대상은 `thirdparty`뿐이다.
```
source_type = official | coop | dorm | council | thirdparty
```

**시설 별칭 정정** — 원천 표기는 **`진수원`**이다. 설계문서의 `진수당`은 내 오기이니 양쪽 다 별칭에 넣는다.
`restNm`은 `의대식당`인데 생협 JS는 `의대`로 매칭하므로 둘 다 등록한다.

> **후생관은 방학에도 점심을 운영한다.** 8/10 점심 11개 코너 정상. 조식·석식만 미운영.
> 생활관이 비어 있다고 후생관도 쉰다고 유추하면 안 된다. §7 참조.

---

## 1. 아키텍처 — 무엇을 만들고 무엇을 안 만드는가

```
[카카오 오픈빌더]  발화 → 블록 매칭 + 엔티티 추출        ← 플랫폼이 함. 우리는 안 만든다
        ↓ action.params / detailParams
[스킬서버]        params → 온톨로지 조회 → 게이트 → 템플릿  ← 우리가 만든다. 순수 결정론
        ↑
[크롤러/어드민]   T1~T4 데이터 공급                       ← 우리가 만든다
```

**실시간 답변 경로에 LLM이 없다.** 이건 방침이 아니라 구조다.
LLM은 오프라인 용도로만 쓴다(폴백 로그 분석, 별칭 후보 제안).

### 스택
Python 3.11+ / FastAPI / SQLite(1단계) → Postgres(3단계) / httpx + selectolax / Playwright(필요 시) / pytest

```
jbnu-chatbot/
├── config/
│   ├── sources.yaml          # 원천 정의 (URL, 티어, 주기, 파서, 폴백)
│   ├── selectors.yaml        # CSS 셀렉터 ★코드에 박지 말 것
│   └── aliases.yaml          # 별칭 사전 → 오픈빌더 엔티티로 export
├── crawler/
│   ├── fetch.py              # HTTP/Playwright, content_hash, 스냅샷 저장
│   ├── parsers/
│   │   ├── coop_week_menu.py
│   │   ├── likehome_week_menu.py
│   │   └── jbnu_notice.py    # 2단계
│   ├── validate.py           # 검증 게이트
│   ├── watch.py              # T3 원문 해시 감시 / T4 만료 점검
│   └── run.py                # CLI 진입점
├── store/
│   ├── schema.sql
│   ├── models.py
│   └── repo.py               # upsert / 신선도 질의
├── skill/
│   ├── server.py             # FastAPI, 카카오 스킬 엔드포인트
│   ├── handlers/             # 블록별 핸들러
│   ├── templates.py          # 응답 템플릿 ★생성 모델 금지 구역
│   ├── kakao.py              # 응답 빌더 (UI 제약 강제)
│   └── safety.py             # D11 안전 분기 ★인텐트보다 먼저
├── admin/                    # 3단계. T4 입력 UI
├── tests/
└── docs/source_probe.md
```

---

## 2. DB 스키마

1단계에서 식단만 쓰더라도 **테이블은 전 도메인 구조로 만든다.** 나중에 마이그레이션하는 비용이 훨씬 크다.

```sql
CREATE TABLE source_snapshot (
  id            TEXT PRIMARY KEY,
  source_key    TEXT NOT NULL,
  url           TEXT NOT NULL,
  fetched_at    TEXT NOT NULL,
  http_status   INTEGER,
  content_hash  TEXT NOT NULL,         -- 원문 바이트 해시 (감사용)
  stable_hash   TEXT NOT NULL,         -- ★변동분(캐시버스터 등) 제거 후 해시. 변경 감지는 이걸로
  content_path  TEXT NOT NULL,         -- ★저장은 항상 원문 그대로. 정규화본을 저장하지 않는다
  media_type    TEXT NOT NULL          -- html | image | json | pdf
);
-- 정규화본을 저장하면 나중에 파서를 고쳐 재파싱할 때 원본이 없다. 비교용 정규화는 메모리에서만.

CREATE TABLE organization (
  id TEXT PRIMARY KEY, name TEXT NOT NULL, aliases TEXT,
  type TEXT NOT NULL,                  -- 총학생회|단과대학생회|생협|생활관|본부부서|동아리연합회
  parent_org_id TEXT REFERENCES organization(id),
  official_url TEXT, contact_id TEXT
);

CREATE TABLE place (
  id TEXT PRIMARY KEY, name TEXT NOT NULL, aliases TEXT,
  campus TEXT, building TEXT, floor TEXT, lat REAL, lng REAL, entrance_note TEXT
);

CREATE TABLE facility (
  id TEXT PRIMARY KEY, name TEXT NOT NULL, aliases TEXT,
  place_id TEXT REFERENCES place(id),
  operated_by TEXT REFERENCES organization(id),
  facility_type TEXT NOT NULL,         -- 열람실|강의실|체육시설|식당|카페|매점|사무실
  capacity INTEGER, reservation_required INTEGER DEFAULT 0, reservation_url TEXT,
  source_url TEXT NOT NULL,
  source_type TEXT NOT NULL            -- official|coop|council|thirdparty
);
-- 식당·카페는 facility_type='식당'|'카페'. 별도 outlet 테이블을 두지 않는다.

CREATE TABLE operating_hours (
  id TEXT PRIMARY KEY,
  facility_id TEXT NOT NULL REFERENCES facility(id),
  term TEXT NOT NULL,                  -- 학기중|방학|시험기간|unspecified  ★원천이 안 밝히면 unspecified
  weekday INTEGER NOT NULL,            -- 0=일 .. 6=토, 7=공휴일
  meal_type TEXT NOT NULL DEFAULT '',  -- ★breakfast|lunch|dinner|'' (끼니 무관)
  open_time TEXT, close_time TEXT, break_start TEXT, break_end TEXT, note TEXT,
  -- 공통 출처 메타 (아래 8개는 모든 fact 테이블에 동일하게 들어간다)
  source_id TEXT NOT NULL REFERENCES source_snapshot(id),
  source_url TEXT NOT NULL, observed_at TEXT NOT NULL,
  valid_from TEXT NOT NULL, valid_to TEXT,
  confidence REAL NOT NULL, extraction_method TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'verified',   -- verified|quarantine|needs_review|superseded|expired
  tier TEXT NOT NULL                          -- T1|T2|T3|T4
);

CREATE TABLE meal_service (
  id TEXT PRIMARY KEY,
  facility_id TEXT NOT NULL REFERENCES facility(id),
  date TEXT NOT NULL,                  -- YYYY-MM-DD
  meal_type TEXT NOT NULL,             -- breakfast|lunch|dinner
  service_status TEXT NOT NULL,        -- operating|closed_temporary|unknown  ★closed_vacation/holiday 폐기
  zone   TEXT NOT NULL DEFAULT '',     -- 1단 구역 (한식 / 찌개*돌솥 / 일품 …)
  corner TEXT NOT NULL DEFAULT '',     -- 2단 코너 (백반 / 찌개 / 돌솥 …)
  note TEXT,                           -- ★price_default 삭제. 날짜 키 테이블에 가격을 두지 않는다
  source_id TEXT NOT NULL REFERENCES source_snapshot(id),
  source_url TEXT NOT NULL, observed_at TEXT NOT NULL,
  valid_from TEXT NOT NULL, valid_to TEXT,
  confidence REAL NOT NULL, extraction_method TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'verified', tier TEXT NOT NULL,
  UNIQUE(facility_id, date, meal_type, zone, corner)
);
-- ★ zone/corner는 반드시 NOT NULL DEFAULT ''. NULL이면 UNIQUE가 무력화돼(SQLite·Postgres 공통)
--   같은 조합이 매 크롤마다 중복 삽입된다. 조용히 데이터가 불어나는 유형의 버그다.

CREATE TABLE menu_item (
  id TEXT PRIMARY KEY,
  meal_service_id TEXT NOT NULL REFERENCES meal_service(id),
  name TEXT NOT NULL, name_normalized TEXT NOT NULL,
  category TEXT, allergens TEXT, is_vegetarian INTEGER DEFAULT 0,
  display_order INTEGER DEFAULT 0,
  source_id TEXT NOT NULL REFERENCES source_snapshot(id),
  source_url TEXT NOT NULL, observed_at TEXT NOT NULL,
  confidence REAL NOT NULL, extraction_method TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'verified', tier TEXT NOT NULL
);
-- ★ price 컬럼 삭제. 가격은 날짜에 붙어 있지 않다. 아래 menu_price로 분리.

CREATE TABLE menu_price (
  id TEXT PRIMARY KEY,
  facility_id TEXT NOT NULL REFERENCES facility(id),
  name TEXT NOT NULL,
  name_normalized TEXT NOT NULL,       -- 조인 키. 정규화 규칙은 menu_item과 동일해야 한다
  price_text TEXT NOT NULL,            -- ★원문 표기 그대로. "6,000원 - 6,500원", "6,000원 부터"
  price_min  INTEGER NOT NULL,         -- 정렬·검증용
  price_max  INTEGER,                  -- 범위면 상한, 단일가면 min과 동일, '부터'면 NULL
  audience TEXT NOT NULL DEFAULT '전체', -- ★구성원|외부인|전체. 같은 이름에 가격이 둘인 경우
  currency TEXT DEFAULT 'KRW', note TEXT,
  source_id TEXT NOT NULL REFERENCES source_snapshot(id),
  source_url TEXT NOT NULL, observed_at TEXT NOT NULL,
  valid_from TEXT NOT NULL, valid_to TEXT,
  confidence REAL NOT NULL, extraction_method TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'verified',
  tier TEXT NOT NULL DEFAULT 'T2',
  UNIQUE(facility_id, name_normalized, audience, valid_from)
);
-- ★ audience를 UNIQUE에 넣지 않으면 "구성원 7,000 / 외부인 8,500"에서 한쪽이 다른 쪽을 덮어쓴다.
--   외부인 행이 이기면 학생 전원에게 8,500원을 안내하게 된다. 실제 오답 경로다.
-- ★ 답변은 price_text를 그대로 렌더한다. 범위를 하한 단일값으로 접지 않는다(§가격 표기 규칙).

CREATE TABLE notice (
  id TEXT PRIMARY KEY,
  issuer_org_id TEXT REFERENCES organization(id),
  title TEXT NOT NULL, body TEXT, published_at TEXT NOT NULL,
  period_start TEXT, period_end TEXT, target_audience TEXT, apply_url TEXT,
  supersedes TEXT REFERENCES notice(id),
  source_id TEXT NOT NULL REFERENCES source_snapshot(id),
  source_url TEXT NOT NULL, observed_at TEXT NOT NULL,
  confidence REAL NOT NULL, extraction_method TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'verified', tier TEXT NOT NULL
);

-- 2단계 이후. 1단계엔 테이블만 만들어 둔다.
CREATE TABLE procedure (
  id TEXT PRIMARY KEY, title TEXT NOT NULL, aliases TEXT,
  owner_org_id TEXT REFERENCES organization(id), domain TEXT,
  steps TEXT, required_docs TEXT,       -- JSON
  period_start TEXT, period_end TEXT, eligibility TEXT, fee TEXT, contact_id TEXT,
  source_id TEXT NOT NULL REFERENCES source_snapshot(id),
  source_url TEXT NOT NULL, observed_at TEXT NOT NULL,
  valid_from TEXT NOT NULL, valid_to TEXT,
  confidence REAL NOT NULL, extraction_method TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'verified', tier TEXT NOT NULL
);

CREATE TABLE pledge (
  id TEXT PRIMARY KEY, term INTEGER NOT NULL, number INTEGER NOT NULL,
  category TEXT, title TEXT NOT NULL, summary TEXT, target_date TEXT
);

CREATE TABLE pledge_progress (
  id TEXT PRIMARY KEY,
  pledge_id TEXT NOT NULL REFERENCES pledge(id),
  status TEXT NOT NULL,                 -- 계획|진행중|완료|보류|철회
  progress_note TEXT, evidence_url TEXT,
  author TEXT NOT NULL, approved_by TEXT NOT NULL,   -- ★T4 필수
  source_id TEXT NOT NULL REFERENCES source_snapshot(id),
  source_url TEXT NOT NULL, observed_at TEXT NOT NULL,
  valid_from TEXT NOT NULL,
  valid_to TEXT NOT NULL,               -- ★T4는 NOT NULL. 만료 없는 수기 정보 금지
  confidence REAL NOT NULL, extraction_method TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'verified', tier TEXT NOT NULL DEFAULT 'T4'
);

CREATE TABLE alias (
  surface_form TEXT NOT NULL, canonical_id TEXT NOT NULL,
  entity_type TEXT NOT NULL, weight REAL DEFAULT 1.0,
  PRIMARY KEY (surface_form, canonical_id)
);

CREATE TABLE crawl_run (
  id TEXT PRIMARY KEY, source_key TEXT NOT NULL,
  started_at TEXT NOT NULL, finished_at TEXT,
  outcome TEXT NOT NULL,                -- success|unchanged|parse_error|fetch_error|quarantined
  items_parsed INTEGER DEFAULT 0, items_quarantined INTEGER DEFAULT 0,
  error_message TEXT
);

CREATE INDEX idx_meal_lookup ON meal_service(facility_id, date, meal_type);
CREATE INDEX idx_menu_meal   ON menu_item(meal_service_id);
CREATE INDEX idx_alias       ON alias(surface_form);
CREATE INDEX idx_notice_pub  ON notice(published_at DESC);
```

### 가격 조인 규칙 ★새로 확정

단가표 이름과 식단표 이름이 안 맞는다(`등심돈까스` ↔ `통등심돈까스`, `계란라면` ↔ `신라면`).

- **정확 일치(`name_normalized` 완전 동일)만 가격을 붙인다.** 유사도·부분일치·LLM 매칭 전부 금지
- 정규화는 공백·괄호 제거 수준까지만. `통`, `신` 같은 접두어를 벗기는 규칙을 만들지 말 것 — `신라면`과 `계란라면`은 실제로 다른 상품이다
- 미매칭 항목은 `price = NULL`. 답변에서 `—`로 표시하고 값을 만들어내지 않는다
- **`price_match_rate`를 크롤 지표로 기록한다.** 이 값이 갑자기 떨어지면 단가표나 식단표의 작명 규칙이 바뀐 신호다. 임계 이하면 알림

이건 "가격을 못 붙인다"가 아니라 **추론으로 채운 값은 사실이 아니다**의 적용이다. 조인은 추론이므로, 결정론적으로 확실할 때만 한다.

### 가격 표기 규칙 ★새로 확정

단가표 34행 중 일부가 단일 정수가 아니다.
```
스페셜오므라이스  6,000원 - 6,500원      → price_text 그대로, min 6000 / max 6500
찌개              6,000원 부터           → price_text 그대로, min 6000 / max NULL
유부우동          3,000원 (곱빼기 +500)  → price_text 그대로, min 3000 / max 3000, note에 옵션
진수원 백반       구성원 7,000 / 외부인 8,500 → 2행. audience로 구분
```

**범위를 하한 단일값으로 접지 않는다.** 6,000~6,500원짜리를 "6,000원"이라고 답하면, 학생이 6,000원 들고 갔다가 모자란다. 가격을 낮게 말하는 건 높게 말하는 것보다 나쁘다.

`price_text`를 그대로 렌더하고, `price_min`/`price_max`는 검증·정렬 전용으로만 쓴다.
`audience`는 답변에서 **구성원 가격을 기본**으로 하되, 외부인 가격이 따로 있으면 `note`에 병기한다(학생이 사용자이므로).

`normalize_name`은 괄호 문자만 지우고 안의 내용은 남긴다. 내용까지 지우면 `오므라이스류(기본)` → `오므라이스류`가 되어 다른 상품에 가격이 붙는다. 계약의 "괄호 제거"는 이 좁은 해석이 맞다.

### 답변 UI 분리 ★실측 반영 (match_rate 68%)

메뉴 카드에 가격을 섞지 않는다. 별도 블록으로 뺀다.
```
food.menu.today  → 메뉴만 (zone 표시)  + quickReply "가격 얼마야"
food.price       → 매칭된 항목만 카드에. 미매칭은 카드에 넣지 않는다
                   footer: "그 외 N개는 단가표에서 확인" + 원문 링크
```

**미매칭 항목을 `—`로 나열하지 않는다.** 실측 매칭률이 68%라 카드의 3분의 1이 `—`가 되는데, 그러면 챗봇이 고장난 것처럼 보인다.
없는 값을 만들지 않는다는 원칙은 그대로 지키되(빈 값을 지어내지 않음), 개수를 밝히고 원문으로 넘긴다.

`price_match_rate`의 **정상 상태가 68%**다. 100%가 목표가 아니다. 이 값이 떨어질 때가 작명 규칙 변경 신호다.

### 교차 검증 ★실측 기준선 반영 (90칸)

| | 원천 | 비고 |
|---|---|---|
| 1차 | 생협 JSON API | 날짜 지정 가능, 안정적 |
| 2차 | 학교 XHR | 이번 주만. 가격·운영시간 보유 |

**관측의 부재는 충돌이 아니다.** 침묵과 진술은 서로 모순하지 않는다.
우선순위 규칙은 **둘 다 관측일 때만** 발동한다.

```
둘 다 관측  → 진짜 충돌. 1차 채택 + conflict 플래그 + 경보
한쪽만 관측 → 충돌 아님. 관측한 쪽 채택 (1차·2차 무관)
둘 다 미관측 → unknown
```

기존 규칙("불일치 시 무조건 1차")은 이 케이스에서 **관측이 관측의 부재에 지는** 결과를 낳는다.
생협은 `diet:""`로 아무 말도 안 했고, 학교는 `운영없음`을 명시했다. 후자를 버릴 이유가 없다.

**채택한 레코드의 `source_id`·`source_url`은 실제 채택된 원천을 가리켜야 한다.** 답변에 링크가 나가므로.

### 지표 분리

`absence_vs_observation`은 충돌이 아니므로 `conflict_rate` 분자에서 **뺀다.** 별도 지표로 둔다.

| 지표 | 실측 기준선 | 경보 |
|---|---|---|
| `content_conflict_rate` | **0.0%** | ★여기에 건다. 0이 정상이므로 1건만 나와도 신호 |
| `coverage_gap_rate` (구 `absence_vs_observation`) | 11.1% (10/90) | 경보 아님. 추세만 관찰 |

`conflict_rate` 하나로 경보를 걸면 상시 11%라 경보가 안 울리는 것과 같다.

### 불변 규칙 — 절대 완화 금지

- 모든 fact 테이블의 `source_id`, `source_url`, `observed_at`, `confidence`, `extraction_method`, `tier` = NOT NULL
- **T4 레코드는 `valid_to`, `author`, `approved_by`가 NOT NULL** — 만료 없는 수기 정보는 저장 불가
- 검증 실패 레코드는 삭제하지 않고 `status='quarantine'`
- 조회 쿼리는 항상 `status='verified'` 필터. 예외 없음

---

## 3. 크롤러 CLI

```bash
python -m crawler.run --source coop_week_menu
python -m crawler.run --source coop_week_menu --date 2026-09-01
python -m crawler.run --all
python -m crawler.run --source coop_week_menu --dry-run
python -m crawler.watch --tier T3      # 원문 해시 감시
python -m crawler.watch --expire       # T4 만료 점검
```

### 동작 순서
1. fetch → `content_hash` 계산
2. 직전 스냅샷과 동일 → `observed_at`만 갱신 (outcome=`unchanged`)
3. 변경 → parse → validate → upsert
4. 실패 → `crawl_run.outcome` 기록 + 경고. **기존 데이터는 건드리지 않는다**

### 파서 출력 계약 ★개정

```python
@dataclass
class ParsedMeal:
    facility_id: str
    date: str                 # YYYY-MM-DD
    meal_type: str            # breakfast|lunch|dinner
    service_status: str       # operating|closed_temporary|unknown   ← closed_vacation/holiday 제거
    zone: str                 # NOT NULL. 없으면 ''
    corner: str               # NOT NULL. 없으면 ''
    items: list[ParsedItem]
    confidence: float
    extraction_method: str

@dataclass
class ParsedItem:
    name: str
    category: str | None
    display_order: int
    # price 없음 — menu_price로 분리됨
```

### 빈 칸 처리 규칙 ★전면 개정

이전 규칙(빈 칸 → `closed_vacation`)은 **폐기한다.** §5 추론 금지와 충돌한다.

빈 칸은 관측값이 아니라 **관측의 부재**다. 그리고 같은 빈 칸이 원천마다 뜻이 다르다.

| 사례 | 실제 의미 |
|---|---|
| 생활관 8/10 전 칸 | 방학 미운영 |
| 진수원 조식 | 애초에 조식을 안 하는 식당 |
| 의대식당 석식 | 위와 같음 |

셋을 구분하려면 다른 원천(운영시간표)을 봐야 한다. 파싱 시점에 `closed_vacation`을 쓰는 순간 그게 추론이다.

**개정 규칙**

| 관측 | `service_status` |
|---|---|
| 메뉴 있음 | `operating` |
| 칸이 비어 있음 | **`unknown`** |
| 원천이 "운영없음"을 **명시** | `closed_temporary` |
| 표 자체를 못 찾음 | 레코드 생성 안 함, `outcome='parse_error'` |

"운영하지 않아요"(B분기) 판단은 **답변 시점에 `operating_hours`와 조인해서** 한다. 사실은 원천별로 순수하게 두고, 결합은 질의 계층에서.

### 판정 우선순위 ★확정

시간표 조인은 **마지막 수단**이다. 관측이 추론을 항상 이긴다.

```
1. 메뉴가 관측됨            → A          (시간표 무시)
2. 원천이 미운영을 명시     → B          (시간표 무시)
3. 그 외 unknown            → 시간표 조인 → B / C-1 / C-2
```

그래서 `serves_meal()`이 `None`인데 A가 나오는 건 정상이다. 시간표는 "한다"고 말하지 않았지만(비대칭 규칙으로 강등), 메뉴가 실제로 관측됐으므로 답할 수 있다.

### 부정도 관측이다 — `operating_hours.is_closed`

"주말·공휴일 미운영"은 원천이 **명시한 부정**이다. 행을 안 만들면 "안 함"과 "안 긁음"이 같은 모양이 된다.

```sql
ALTER TABLE operating_hours ADD COLUMN is_closed INTEGER NOT NULL DEFAULT 0;
```

이게 되면 요일 차원에서는 폐쇄세계 가정이 **아예 필요 없어진다.** 미운영도 관측으로 답한다.
`hours_coverage='complete'`가 실제로 여는 건 끼니 차원 하나다(진수원 시간표에 조식 행 없음 → 조식 안 함).

### `hours_coverage='complete'` 판정 기준 (확정)

셋을 모두 만족해야 한다. 하나라도 안 되면 `partial`로 남는다.

1. 시간표 파싱 성공, 행 1건 이상
2. **요일 차원이 빠짐없다** — 게시된 각 끼니에 대해 일~토 7개가 여는 행 또는 `is_closed=1` 행으로 채워짐
3. 그 표가 해당 시설이 게시한 운영시간 전부다

**파싱 성공만으로 `complete`를 세우면 안 된다.** 일부만 긁힌 표를 `complete`로 두면 "행 없음"이 곧 "안 함"이 되어, 운영 중인 끼니를 미운영으로 답한다.

### B분기 판정표 (질의 계층)

```python
serves = repo.serves_meal(facility_id, date, meal_type)   # True | False | None
```

| meal_service | serves | 답변 |
|---|---|---|
| `operating` + items | — | **A** 메뉴 |
| `closed_temporary` | — | **B** "오늘은 쉬어요" |
| `unknown` | `False` | **B** "○○은 조식은 운영하지 않아요" |
| `unknown` | `True` | **C-1** "아직 올라오지 않았어요" |
| `unknown` | `None` | **C-2** "확인하지 못했어요" |

C-1과 C-2를 나누는 게 중요하다. 운영하는 걸 아는데 메뉴만 없는 것과, 아무것도 모르는 건 다른 상태다.
`serves_meal()`이 데이터 없을 때 `False`가 아니라 `None`을 돌려주는 것 — 그대로 유지할 것. 모를 때 "안 해요"라고 답하는 게 추론이다.

### `term` 배정 규칙 ★확정

학교 XHR 운영시간표에는 term이 없다(`평일 11:30~14:00 / 주말·공휴일 미운영`만 있음).

**(a) `학기중` 고정도, (b) `''`(무관)도 안 된다. 둘 다 원천이 말하지 않은 걸 채운다.**
(a)는 없는 범위를 좁히고 (b)는 없는 범위를 넓힌다. 방향만 다를 뿐 같은 위반이다.

**결정: `term = 'unspecified'`로 저장한다.** 미상을 미상으로 적는다. 그리고 **쓰는 방식을 질의 계층에서 방향에 따라 나눈다.**

```
serves_meal(facility, date, meal_type) with term='unspecified' 행만 있을 때

  미운영 결론 (negative) → 채택        → B
  운영 결론  (positive)  → None으로 강등 → C-2
```

근거는 **단조성**이다. 방학에 운영이 줄지 늘지는 않는다(실측: 후생관 방학 조식·석식 `운영없음`).
따라서 term 미상 시간표에서 나온 **"안 한다"는 방학에도 성립**하지만, **"한다"는 성립하지 않을 수 있다.**

비대칭이 결정적이다.
- 긍정을 잘못 쓰면 → 학생이 6시에 갔는데 문이 닫혀 있다. **우리가 보낸 것이다**
- 부정을 잘못 쓰면 → 학생이 안 간다. 원문 링크는 같이 나가므로 확인할 수 있다

헛걸음을 만드는 쪽이 훨씬 나쁘다. 그래서 긍정 방향만 강등한다.

원천이 **명시한** 미운영(`[지방선거]`, `식사없음`)은 term과 무관하게 항상 이긴다. 그건 추론이 아니라 관측이다.

학사일정(T3)이 들어오면 `unspecified` 행을 실제 term으로 승격시키고 강등 규칙을 푼다. 그때까지는 이걸로 간다.

### `hours_coverage` — 행이 없는 것과 안 하는 것 ★추가

`토요일 저녁 (운영시간 행 없음) → serves=False`는 **완전 수집을 전제한 폐쇄세계 가정**이다.
행이 없는 게 "안 한다"인지 "아직 안 긁었다"인지 구분해야 한다.

```sql
ALTER TABLE facility ADD COLUMN hours_coverage TEXT NOT NULL DEFAULT 'partial';
-- complete: 이 시설의 운영시간표를 통째로 수집함 → 행 없음 = 미운영
-- partial : 일부만 있음                        → 행 없음 = 모름(None)
```

`complete`는 시간표 전체를 파싱한 크롤러만 세울 수 있다. 기본값은 `partial`이다.

> **생기는 의존성**: 이제 `operating_hours`가 식단 답변 품질의 전제다. 작업 순서 4번에서 같이 수집할 것.

### 검증 게이트 (`validate.py`)

```python
PRICE_MIN, PRICE_MAX = 500, 20000
NAME_LEN_MIN, NAME_LEN_MAX = 1, 40
ANOMALY_DELTA = 0.7
```
스키마 → 도메인 규칙 → 이상 탐지(0건인데 이전엔 정상 / 중복 3개↑ / 항목 수 급변) → T3·T4 전용 규칙
실패 시 `quarantine` + `crawl_run.items_quarantined` 증가.

---

## 4. 스킬서버

### 엔드포인트
```
POST /skill/{block_name}      # 블록별로 분리
GET  /health
GET  /admin/freshness         # 소스별 마지막 성공 크롤, stale 여부
```

### 오픈빌더 스킬 등록 화면 실측 (2026-08-10 직접 확인)

봇 생성 완료. `bot_id = 6a79bc40b11ba04bddf0a2b7`

스킬 등록 시 입력하는 필드는 이렇다.

| 필드 | 비고 |
|---|---|
| 스킬명 / 설명 | — |
| **URL** (필수) | 운영 엔드포인트 |
| **Test URL** | ★운영과 **별도**로 지정 가능 |
| **헤더값** Key/Value ×3 | ★커스텀 헤더 3개까지 |
| 테스트 헤더값 ×3 | 테스트용 별도 |
| 기본 스킬로 설정 | 체크박스 |
| 버전 | 자동 |

**여기서 나온 요구사항 두 개**

1. **스킬서버 인증을 반드시 붙인다.** 스킬 엔드포인트는 공개 HTTPS라 주소만 알면 누구나 호출할 수 있다.
   커스텀 헤더가 지원되므로 공유 시크릿으로 막는다.
   ```
   헤더:  X-Skill-Token: <랜덤 32바이트>
   서버:  헤더 불일치 → 401, 로그 기록
   ```
   시크릿은 환경변수로. 저장소에 커밋하지 않는다.

2. **Test URL이 별도라 개발 채널 없이도 운영/테스트 분리가 된다.**
   `POST /skill/...`(운영) 과 `POST /skill/.../test`(스테이징 DB) 를 나눠 두면
   봇 테스트에서 실 데이터를 건드리지 않고 검증할 수 있다.

### 요청 처리 순서 ★순서가 중요

```python
def handle(payload):
    utterance = payload["userRequest"]["utterance"]

    # 1. 안전 분기 — 인텐트 분류보다 먼저. 절대 뒤로 옮기지 말 것
    if safety.is_sensitive(utterance):
        return safety.response()          # 상담 창구 연락처만. 대화 이어가지 않음

    # 2. 오픈빌더가 추출한 파라미터 사용 (자체 NLU 없음)
    params = payload["action"]["params"]
    detail = payload["action"]["detailParams"]

    # 3. 온톨로지 조회 + 게이트
    facts = repo.query(block, params)     # status='verified' AND 신선도 통과

    # 4. 템플릿 렌더
    return templates.render(block, facts)
```

`payload` 주요 필드 (공식 규격)
```
userRequest.utterance          사용자 발화
userRequest.user.id            botUserKey (봇별 고유, 비식별)
action.params                  엔티티 추출값 (JSON 문자열일 수 있음)
action.detailParams            추출값 + origin(원래 발화 표현)
flow.trigger.type              TEXT_INPUT / QUICKREPLY_BUTTON_MESSAGE 등
```
> `sys.date`를 쓰면 "금요일"이 `{"date": "2026-08-14", ...}` JSON 문자열로 온다. `json.loads` 후 `date` 키를 쓴다.
> `flow.trigger.type`을 로깅하면 **타이핑 발화와 버튼 클릭을 구분**할 수 있다. 폴백 개선의 핵심 지표다.

### 응답 3분기 (반드시 셋 다 구현)

**A. 사실 있음 + 신선함**
```json
{
  "version": "2.0",
  "template": {
    "outputs": [{ "listCard": {
      "header": { "title": "후생관 오늘 점심 (8/10)" },
      "items": [
        { "title": "스페셜오므라이스", "description": "3,800원" },
        { "title": "제육덮밥", "description": "3,000원" }
      ],
      "buttons": [{ "action": "webLink", "label": "원문 보기",
                    "webLinkUrl": "https://coopjbnu.kr/menu/week_menu.php" }]
    }}],
    "quickReplies": [
      { "label": "내일 메뉴", "action": "message", "messageText": "후생관 내일 메뉴" },
      { "label": "운영시간", "action": "message", "messageText": "후생관 몇 시까지 해?" }
    ]
  }
}
```
관측 시각(`8/10 06:00 확인`)을 반드시 노출한다.

**B. 명확한 미운영** (`service_status`가 `closed_*`)
```
"후생관은 방학 기간이라 오늘은 운영하지 않아요. 개강(9/1)부터 다시 안내해 드릴게요."
```

**C. 확인 불가** (`unknown` / stale / `quarantine` / `needs_review` / T4 만료)
```
"오늘 후생관 식단을 아직 확인하지 못했어요.
 생협 홈페이지에서 직접 확인해 주세요 → (링크)
 문의: 생협 063-270-2151"
```
**추측한 값을 절대 내놓지 않는다.** C가 이 프로젝트의 핵심 기능이다.

### `kakao.py` — UI 제약을 코드로 강제

빌더 함수에서 넘치면 자동으로 자르거나 예외를 던진다. 사람이 매번 기억할 수 없다.

| 항목 | 제한 |
|---|---|
| `outputs` | 1~3개 |
| `quickReplies` | ≤10 |
| `simpleText.text` | ≤1000자 (500자 초과 시 접힘) |
| **`listCard.items`** | **≤5** (케로셀 4) — 초과 시 5개 + "전체 보기" 버튼 |
| `textCard` | title ≤50 / desc ≤400 |
| `basicCard` | title ≤50 / desc ≤230 / thumbnail 필수 |
| 버튼 | 가로 ≤2(라벨 8자) / 세로 ≤3(라벨 14자) |
| 케로셀 | ≤10 (listCard ≤5) |
| 루트 | `"version": "2.0"` 필수 |

---

## 5. 수용 기준

| # | 테스트 | 기대 |
|---|---|---|
| T1 | 정상 주간표 픽스처 | 요일×끼니 개수 일치 (가격 검증은 T16으로 이동) |
| T2 | **빈 칸 픽스처** | `service_status='unknown'`, `parse_error` 아님 |
| T2b | **빈 칸 + `serves_meal()` 3분기** | `False`→B / `True`→C-1 / `None`→C-2 |
| T3 | **셀렉터 깨진 HTML** | `outcome='parse_error'`, **기존 DB 무변경** |
| T4 | 가격 999,999 포함 | 해당 아이템만 `quarantine`, 나머지 정상 |
| T5 | 동일 hash 재크롤 | `outcome='unchanged'`, 파서 미호출 |
| **T5b** | **`?ver=<타임스탬프>` 포함 실사이트 2회 연속 fetch** | `stable_hash` 동일 → `unchanged`. 원문 해시는 달라도 됨 |
| **T19** | **`term='unspecified'` 시간표만 있을 때** | 미운영 결론→B / 운영 결론→C-2 (강등) |
| **T20** | **`hours_coverage='partial'` + 행 없음** | `serves_meal()`이 `False`가 아니라 `None` |
| T6 | `observed_at` 25시간 전 | C분기(확인 불가) 응답 |
| T7 | 응답 시간 | 100회 p95 < 300ms |
| T8 | 응답 JSON | 카카오 스킬 규격 검증 통과 |
| T9 | `source_id` NULL 삽입 | DB 제약 위반으로 실패 |
| T10 | 별칭 "후생"/"푸드코트"/"공대식당" | 모두 같은 facility_id |
| **T11** | **T4 레코드에 `valid_to` 없이 삽입** | **DB 제약 위반으로 실패** |
| **T12** | **`valid_to` 지난 T4 레코드** | 조회 결과에서 제외 + 만료 알림 |
| **T13** | **민감 발화(인권·성폭력 키워드)** | 인텐트 분류 없이 즉시 안전 응답 |
| **T14** | **메뉴 7개인 listCard** | 5개 + "전체 보기" 버튼으로 렌더 |
| **T15** | **행/열 정렬 회귀** — 공휴일이 포함된 주 픽스처 | 알려진 날짜(예: 지방선거일)가 정확한 요일 칸에 들어감 |
| **T16** | **가격 조인** — `등심돈까스` / `계란라면` 포함 | 둘 다 미매칭. 유사 이름에 붙지 않음 |
| **T16b** | **가격 표기** — 범위·'부터'·구성원/외부인 픽스처 | `price_text` 원문 유지, 범위가 하한으로 접히지 않음, 구성원가 우선 노출 |
| **T17** | **zone/corner 중복** — 같은 주간표 2회 크롤 | 행 수 불변 (UNIQUE가 실제로 동작) |
| **T18** | **교차검증** — 1차·2차 메뉴가 다른 픽스처 | 1차 채택 + `conflict` 플래그 기록 |

**T2·T3·T6·T9·T11·T12·T13·T15·T16이 이 프로젝트의 정체성이다.** 나머지는 평범한 크롤러 테스트고, 이 아홉이 "정확한 정보만"과 "안전"을 코드로 증명하는 부분이다.

### T15가 왜 필요한가

`selectolax`의 `row.css("td,th")`는 **문서 순서를 지키지 않는다.** 셀렉터별로 묶어 반환해 행 머리글이 끝으로 밀린다.
예외도 안 나고 파싱도 "성공"하는데 메뉴가 하루씩 밀린다. **검증 게이트가 못 잡는 유형의 오류다** — 스키마도 맞고 값도 정상 범위이고 개수도 맞는다.

→ `row.iter()` 순회로 고정하고, **외부에서 검증 가능한 앵커**(공휴일·선거일 등 실제 날짜가 알려진 값)로 정렬을 확인하는 회귀 테스트를 둔다.

일반화하면 이렇다. **"파싱 성공 + 값 정상 + 정렬 틀림"은 게이트를 통과한다.** 조용히 틀리는 오류이므로 별도 앵커 검증이 필요하다.

---

## 6. 작업 순서 ★4번 완료 후 개정

1. ~~0장 조사 3건~~ 완료
2. ~~`schema.sql` + `repo.py` + T9, T11~~ 완료
3. ~~`likehome` 파서 파이프라인 (T1~T5, T15)~~ 완료
4. ~~학교 XHR 파서 + 운영시간 수집 (T19, T20)~~ 완료
5. ~~생협 JSON 파서 + 교차검증 (T18)~~ 완료
6. ~~`safety.py` (T13)~~ 완료 — `deployable=False` 상태
7. ~~`kakao.py` 응답 빌더 (T14, T16b)~~ 완료
8. ~~스킬서버 3분기 (T6~T8) + 문안 덤프 검수~~ 완료
9. ~~스케줄러 + 하트비트 + 실사이트 스모크(T5b)~~ 완료
10. **★Render 배포 + Cron Job 이관** ← 다음. 순서를 앞으로 당김
11. **★D1 학사일정 크롤러** (수요 1·3위)
12. **★`deadline.upcoming` 인텐트** — 마감 임박 통합 조회
13. D2 장학·등록금 마감 (수요 2위)
14. `crawler.watch` — T3 해시 감시 / T4 만료 (T12) ← **T3·T4 데이터가 생긴 뒤로 미룸**

### 순서를 바꾼 이유

`crawler.watch`(구 10번)를 뒤로 미룬다. **감시할 대상이 아직 없다.** T3(규정 문서)도 T4(어드민 입력)도 레코드가 0건이라, 지금 만들면 픽스처로만 검증된다.
캐시버스터·PK 충돌 건에서 배운 대로 **픽스처 검증은 불충분하다.** 실데이터가 생긴 뒤에 만든다.

배포를 앞으로 당기는 이유는 시간이다. 노트북 작업 스케줄러는 **노트북이 꺼져 있으면 안 돈다.** 개강까지 3주뿐인 관측 창을 노트북 가동 시간에 걸 수 없다.

### 배포 스펙 ★확정 (2026-08-10)

유료 호스팅으로 간다. 월 1만원 안팎.

| | 결정 |
|---|---|
| 플랫폼 | **Render Starter (정액 ~$7/월)** |
| 이유 | 사용량 과금이 아니라 **정액**. 총학은 매년 사람이 바뀌므로 예측 가능한 고정비가 인수인계에 안전하다 |
| 런타임 | Python / FastAPI. `render.yaml` + `Dockerfile` 또는 네이티브 빌드 |
| DB | 1단계는 SQLite(디스크 마운트). 트래픽이 늘면 Postgres로 |
| 크롤 스케줄 | **웹 서비스 안의 인프로세스 스케줄러**. ★Render Cron Job은 디스크를 못 붙여 SQLite를 볼 수 없다 |
| 인스턴스 수 | **`numInstances: 1` 고정. autoscaling 금지** — 요금 문제가 아니라 **SQLite 동시 쓰기로 데이터가 깨진다** |
| 정적 페이지 | GitHub Pages(무료). `listCard` 5개 제한 초과분의 "전체 보기" 대상 |
| 도메인 | 당분간 플랫폼 서브도메인 사용. 필요해지면 구매 |

### 지출 통제 — 계정 상한은 존재하지 않는다 ★정정

처음에 "플랫폼에서 지출 상한을 걸어라"라고 지시했는데 **그런 기능이 없다.** Render의 spend limit은 빌드 파이프라인 분에만 적용되고, 컴퓨트 포함 계정 전체 상한은 기능 요청 단계다.

없는 기능을 가리키는 지시였다 — 방금 체크 대상에 넣은 "**실재하지 않는 곳을 가리키는 안내**" 그 자체다. 실재하는 통제로 대체한다.

| 통제 | 방법 |
|---|---|
| **정액 인스턴스** | Starter는 시간당 정액. 이게 주 안전장치다 |
| 빌드 분 상한 | Billing에서 설정 (유일하게 실재하는 spend limit) |
| Autoscaling 끄기 | `render.yaml`에 `numInstances: 1` 고정 |
| **카드 한도** | 총학 명의, 잔액 낮은 체크카드. 플랫폼이 못 막으면 카드로 막는다 |
| 월 1회 점검 | 인수인계 문서에 포함 |

> **Hobby 워크스페이스는 팀원 1명만 가능하다.** 그래서 총학 공용 계정이 권장이 아니라 **필수**다.
> 개인 계정으로 만들면 인수인계 시 계정 자체를 통째로 넘겨야 한다.

**"전체 보기"를 우리 페이지로 보낸다.**
지금은 `listCard`가 5개에서 잘리면 생협 원문으로 보내는데, 원문은 우리가 통제할 수 없고 모바일에서 보기도 나쁘다.
GitHub Pages에 크롤 결과를 그대로 렌더한 페이지를 만들어 거기로 보낸다. 무료이고, 게이트를 통과한 데이터만 실리므로 답변과 일관된다.

### 9번이 급한 이유 — 기한이 있다

**9/1 개강까지 3주.** 지금부터 운영시간을 주간 크롤해 두면 학기 경계 전후 변화를 관측할 수 있고,
그러면 `term='unspecified'`를 **추론이 아니라 관측으로** 해소할 수 있다. 놓치면 다음 기회는 반 년 뒤다.

→ 스케줄러에 **운영시간 주간 크롤(월 05:00)을 반드시 포함**할 것. 변화가 없어도 매주 스냅샷을 남긴다.

### 11~13번 — 수요조사 반영 (N=37)

| 수요 | 응답 | 필요한 것 |
|---|---|---|
| 1위 수강신청 일정 62.2% | | 학사일정 크롤 (T1) |
| 2위 장학금·등록금 59.5% | | 공고 기간 크롤 (T1) |
| 3위 학사일정 56.8% | | 위와 동일 |

**"언제"만 하고 "어떻게"는 나중이다.** 설문 문항이 "수강신청 **일정** 안내", "장학금 및 등록금 **안내**"다.
날짜 조회는 T1 크롤로 싸게 되고, 절차·자격 판정(T3)은 비싸므로 뒤로 미룬다.

원천 후보: `https://www.jbnu.ac.kr/mobile/academic/schedule.do` (학사일정)

### `deadline.upcoming` 스펙

불편 1위가 "중요한 공지를 놓친다"(67.6%)인데 알림은 보내지 않는다. 대신 **채널에 들어온 사람이 반드시 보는 자리**에 이 인텐트를 둔다(리스트 메뉴 최상단, 웰컴 블록).

```
입력  없음 (또는 deadline_window, 기본 14일)
조회  period_end 가 오늘 ≤ x ≤ 오늘+14 인 모든 fact
      (calendar / notice / finance 횡단)
정렬  마감 가까운 순
출력  listCard 최대 5건 + "전체 보기"
      각 항목: 제목 / D-N (마감일)
```

**게이트는 동일하다.** 마감일이 stale이면 그 항목을 넣지 않는다. 마감을 틀리게 말하는 건 이 챗봇에서 가장 비싼 오답이다.

**5번을 7번보다 앞에 두는 이유**

- 계약상 1차 원천은 생협인데 지금 후생관이 2차(학교 XHR)에서만 들어온다. **계약과 구현이 어긋난 상태**다
- 학교 XHR은 `date` 파라미터가 없어 **이번 주만** 긁힌다. 크롤이 하루 밀리면 그 주는 영영 못 채운다. 생협 API는 날짜 지정이 되므로 백필이 가능하다
- 교차검증은 파이프라인 **중심부**에 있어 나중에 끼워넣는 비용이 크다. `kakao.py`는 응답 계층이라 나중에 붙여도 침습적이지 않다
- 파서 로직이 머리에 남아 있을 때 두 번째 파서를 짜는 게 싸다

---

## 7. 하지 말아야 할 것

- **자체 NLU 라우터 구현** — 오픈빌더가 한다. `action.params`를 쓴다
- 셀렉터를 파이썬 코드에 하드코딩 (→ `selectors.yaml`)
- 스킬 핸들러 안에서 크롤링·외부 API 호출
- 파싱 실패 시 이전 데이터 삭제 (→ 유지 + stale)
- 답변 문장을 생성 모델로 생성 (→ 템플릿 전용)
- 3자 사이트(다이닝코드·테이블링 등)를 원천 사용 (→ `source_type='thirdparty'`는 답변 제외)
- 빈 표를 파싱 실패로 처리 (→ T2)
- 안전 분기를 인텐트 분류 뒤로 옮기기 (→ T13)
- 식단 전용 테이블 설계 (→ `facility`로 일반화. 나중 마이그레이션이 훨씬 비싸다)
- **가격 이름 유사도 매칭** (→ T16. 정확 일치만)
- **`service_status`를 학사일정으로 유추** (→ 반드시 원천 관측값. 후생관은 방학에도 점심을 돈다)
- **`zone`/`corner`를 NULL 허용으로 두기** (→ UNIQUE 무력화, 중복 폭증)
- **원천 없는 인텐트 만들기** — "다음 주 메뉴"는 원천에 데이터가 없다(45일 확인, 전부 `list:[]`). 백로그로 보낸다

---

## 8. 나에게 되돌려줄 것

- `docs/source_probe.md` — 조사 3건 결과. 특히 **식단표가 이미지인지 여부**
- 실제 파싱된 샘플 JSON 1주치
- `selectors.yaml`, `aliases.yaml` (오픈빌더 엔티티로 올릴 것)
- 예상과 다른 필드가 나왔으면 그 목록 — 스키마를 고쳐서 다시 내려보낸다
